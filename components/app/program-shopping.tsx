import { addProgramItemAction, addTaskAction, applyRoutineAction, updateProgramStatusAction, updateTaskStatusAction } from "@/app/actions/program";
import { addShoppingItemAction, completeShoppingPurchaseAction } from "@/app/actions/shopping";
import { dateLabel } from "./utils";

export function Program({spaceId,selectedDate,snapshot,caregivers,children,routines,memberName,canEdit,canAct}:any){return <div className="stack"><h1>Programme</h1><div className="grid"><div className="stack"><section className="card"><div className="sectiontitle">Programme du {dateLabel(selectedDate)}</div><div className="list">{snapshot.program.map((p:any)=><div className="item" key={p.id}><div><strong>{p.plannedStart||"—"} · {p.title}</strong><small>{p.description||p.location||""}</small></div>{canAct&&p.status==="PLANNED"&&<div className="row"><form action={updateProgramStatusAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="itemId" value={p.id}/><input type="hidden" name="status" value="DONE"/><button className="btn soft">✓ Fait</button></form><form action={updateProgramStatusAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="itemId" value={p.id}/><input type="hidden" name="status" value="NOT_DONE"/><button className="btn danger">Non fait</button></form></div>}</div>)}</div></section><section className="card"><div className="sectiontitle">Tâches</div><div className="list">{snapshot.tasks.map((t:any)=><div className="item" key={t.id}><div><strong>{t.time||""} {t.title}</strong><small>{t.description||""}</small></div>{canAct&&t.status!=="DONE"&&<form action={updateTaskStatusAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="taskId" value={t.id}/><input type="hidden" name="status" value="DONE"/><button className="btn soft">✓</button></form>}</div>)}</div></section></div>{canEdit&&<div className="stack"><section className="card"><div className="sectiontitle">Ajouter au programme</div><form className="form" action={addProgramItemAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="date" value={selectedDate}/><div className="field"><label>Type</label><select name="type"><option>Repas</option><option>École</option><option>Parc</option><option>Sortie</option><option>Activité</option><option>Piscine</option><option>Sieste</option><option>Bain</option><option>Rendez-vous</option><option>Coucher</option><option>Autre</option></select></div><div className="field"><label>Titre</label><input name="title" required/></div><div className="row"><div className="field" style={{flex:1}}><label>Début</label><input name="start" type="time"/></div><div className="field" style={{flex:1}}><label>Fin</label><input name="end" type="time"/></div></div><div className="field"><label>Description / repas</label><textarea name="description"/></div><div className="field"><label>Lieu</label><input name="location"/></div><div className="field"><label>Enfant(s)</label>{children.map((c:any)=><label className="row" key={c.id}><input type="checkbox" name="childIds" value={c.id}/>{c.firstName}</label>)}</div><div className="field"><label>Intervenant(s)</label>{caregivers.map((m:any)=><label className="row" key={m.id}><input type="checkbox" name="memberIds" value={m.id}/>{memberName(m)}</label>)}</div><button className="btn brandbtn">Ajouter</button></form></section><section className="card"><div className="sectiontitle">Créer une tâche</div><form className="form" action={addTaskAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="date" value={selectedDate}/><div className="field"><label>Tâche</label><input name="title" required/></div><div className="field"><label>Heure (facultative)</label><input name="time" type="time"/></div><div className="field"><label>Intervenant(s)</label>{caregivers.map((m:any)=><label className="row" key={m.id}><input type="checkbox" name="memberIds" value={m.id}/>{memberName(m)}</label>)}</div><div className="field"><label>Enfant(s)</label>{children.map((c:any)=><label className="row" key={c.id}><input type="checkbox" name="childIds" value={c.id}/>{c.firstName}</label>)}</div><button className="btn primary">Créer</button></form></section>{routines.length>0&&<section className="card"><div className="sectiontitle">Appliquer une routine</div><form className="form" action={applyRoutineAction}><input type="hidden" name="spaceId" value={spaceId}/><input type="hidden" name="date" value={selectedDate}/><div className="field"><label>Routine</label><select name="routineId">{routines.map((r:any)=><option key={r.id} value={r.id}>{r.name}</option>)}</select></div><div className="field"><label>Intervenant</label><select name="memberId">{caregivers.map((m:any)=><option key={m.id} value={m.id}>{memberName(m)}</option>)}</select></div><div className="field"><label>Enfant</label><select name="childId">{children.map((c:any)=><option key={c.id} value={c.id}>{c.firstName}</option>)}</select></div><button className="btn soft">Appliquer</button></form></section>}</div>}</div></div>}

export function Shopping({spaceId,snapshot,canAdd,canPurchase,children}:any){const todo=snapshot.shopping.filter((i:any)=>i.status==="TODO");return <div className="grid"><section className="card"><h1>Courses</h1><p className="muted">Une seule chaîne : à acheter → acheté → montant → caisse mise à jour automatiquement.</p><div className="list">{snapshot.shopping.map((i:any)=><div className={`item status-${i.status}`} key={i.id}><div><strong>{i.name}</strong><small>{[i.quantity,i.comment].filter(Boolean).join(" · ")}</small></div><span className="pill">{i.status==="TODO"?"À acheter":"Acheté"}</span></div>)}</div></section><div className="stack">{canAdd&&<section className="card"><div className="sectiontitle">Ajouter un produit</div><form className="form" action={addShoppingItemAction}><input type="hidden" name="spaceId" value={spaceId}/><div className="field"><label>Produit</label><input name="name" required/></div><div className="field"><label>Quantité</label><input name="quantity"/></div><div className="field"><label>Commentaire</label><input name="comment"/></div><div className="field"><label>Enfant (facultatif)</label><select name="childId"><option value="">—</option>{children.map((c:any)=><option key={c.id} value={c.id}>{c.firstName}</option>)}</select></div><button className="btn brandbtn">Ajouter</button></form></section>}{canPurchase&&todo.length>0&&<section className="card"><div className="sectiontitle">Terminer les courses</div><form className="form" action={completeShoppingPurchaseAction}><input type="hidden" name="spaceId" value={spaceId}/><div className="field"><label>Produits achetés</label>{todo.map((i:any)=><label className="row" key={i.id}><input type="checkbox" name="itemIds" value={i.id}/>{i.name}</label>)}</div><div className="field"><label>Montant total</label><input name="amount" type="number" min="0.01" step="0.01" required/></div><div className="field"><label>Libellé</label><input name="description" defaultValue="Courses"/></div><button className="btn primary">Enregistrer l’achat</button></form><p className="muted">Aucun choix “argent parents / argent nounou”. Si la caisse ne suffit pas, la différence devient automatiquement une avance.</p></section>}</div></div>}
